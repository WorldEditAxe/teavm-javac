# Compiler Patches

This source root contains local patch classes that keep TeaVM, Rhino, and TeaVM class library behavior compatible with this compiler build.

The Java packages intentionally match the upstream packages they patch. Keep project-owned compiler code in `../main/java`.
